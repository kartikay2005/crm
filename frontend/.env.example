VITE_API_BASE_URL=http://localhost:8000/api/v1

# Shows a "Demo credentials" hint box on the login page pre-filled with the
# workspace/email/password scripts/seed_demo.py creates, so a recruiter or
# reviewer can log in without needing to ask you for a password. Leave unset
# (or "false") for a real deployment with real user data.
VITE_DEMO_MODE=false
